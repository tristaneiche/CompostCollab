<nav class="navbar navbar-expand-lg bg-body-tertiary" style="background-color: #013135; color:#fff; width: 100%">
    <div  class="container-fluid">
        <a class="navbar-brand" aria-current="true" href="{{ path('home_page') }}">Home</a>

        <div class="collapse navbar-collapse" id="navbarText">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                {% if is_granted('ROLE_USER') is same as(true) %}
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('profil') }}">Profil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('composter') }}">Composteur</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('logout') }}">Déconnexion</a>
                    </li>
                {% endif %}

                {% if is_granted('ROLE_USER') is same as(false) %}
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('login') }}">Connexion</a>
                    </li>
                {% endif %}

                {% if is_granted('ROLE_USER') is same as(true) %}
                    <li class="nav-item" style="padding-left: 450px;padding-right: 450px;">
                        <form class="d-flex" role="search">
                            <input class="form-control me-2" type="search" placeholder="Chercher" aria-label="Search">
                            <button class="btn btn-outline-success" type="submit">Chercher</button>
                        </form>
                    </li>
                {% endif %}

                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="nav-text" style="text-align: right;">
                    <p style="color:#fff"><b>{{ app.user.email }}  {{ app.user.lastname }}</b></p>
                </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>